# 核心框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
aiofiles==24.1.0
aiohttp==3.13.5
websockets==13.0

# 日语处理
fugashi[unidic-lite]==1.4.0
unidic-lite==1.0.8
jaconv==0.4.0

# 视频处理
ffmpeg-python==0.2.0

# 轻量人声分离 (vocal-remover, ~200MB 含 torch CPU)
# 无 GPU 服务器也能跑，CPU 2-3 分钟/首歌
torch>=2.1.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# ============ 以下为重型 AI 模型依赖（较大，按需安装）============
# 取消注释以启用高质量 Demucs 人声分离:
#
# 人声分离 - Demucs (GPU 推荐，质量更好)
# demucs==4.0.1
# torchaudio==2.4.0
#
# 语音识别 & 时间对齐 (约 3GB)
# openai-whisper==20240930
# stable-ts==2.16.0
